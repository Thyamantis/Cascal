# Cascal
An old compiler project intended for an obscure computer architecture called Calibeus, where the paradigm is multi-level logic state machines. This repository applies only to binary state machines.

